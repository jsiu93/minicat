# 本地开发环境配置示例
# 复制此文件为 application-local.yml 并修改为你的实际路径

spring:
  datasource:
    url: jdbc:sqlite:./data/tasks.db

minicat:
  storage:
    base-path: ./data
  logs:
    path: ./logs
  security:
    encryption-key: minicat-default-secret-key-2024

